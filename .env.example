PORT=8080
CORS_ORIGIN=http://localhost:3000

# Postgres (seed test quote + POST /quotes/seed-test)
# Con docker compose en la raíz del repo (usuario/clave/db: pettransfer):
# DATABASE_URL=postgresql://pettransfer:pettransfer@localhost:5432/pettransfer
# Opcional: lote para npm run db:import:quotes-clean (default 150)
# QUOTES_IMPORT_BATCH_SIZE=200

# Opcional (AssemblyAI) cuando integres transcripción
# ASSEMBLYAI_API_KEY=
# AAI_API_KEY=
